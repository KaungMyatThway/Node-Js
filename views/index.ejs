<% if (blogs.length) { %>
  <% blogs.forEach(blog => { %>
    <h1><a href="/blogs/<%= blog._id %>"><%= blog.title %></a></h1>
    <p><%= blog.text %></p>
  <% }) %>
<% } else { %>
  <h1>There is no blog</h1>
<% } %>
